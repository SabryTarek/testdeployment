version: 2.1
orbs:
  ecm: pay-justice/export-commit-message@0.0.1
jobs:
   build:
     parameters:
       elkHost:
         type: env_var_name
         default: ELK_DEFAULT_HOST # Note: replace with env var (without $) or remove it if the param is mandatory.
       ebAppName:
         type: string
         default: 'my-app-name'
     docker:
       - image: ubuntu:latest
     steps:
       - checkout
       - ecm/export_commit
       - run: |
           echo "<< parameters.elkHost >>  << parameters.ebAppName >>"
workflows:
  workflow:
    jobs:
      - build:
          elkHost: GIT_COMMIT_MESSAGE
          ebAppName: tarek