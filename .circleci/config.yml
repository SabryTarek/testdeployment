version: 2.1
orbs:
  ecm: pay-justice/export-commit-message@0.0.1
jobs:
   sa:
     parameters:
       commit-msg:
         type: env_var_name
         default: GIT_COMMIT_MESSAGE # Note: replace with env var (without $) or remove it if the param is mandatory.
       AppName-msg:
         type: string
         default: 'my-app-name'
     docker:
       - image: ubuntu:latest
     steps:
       - checkout
       - ecm/export_commit
       - run: |
           echo "<< parameters.commit-msg >>  << parameters.AppName-msg >>"
workflows:
  workflow:
    jobs:
      - sa:
          commit-msg: GIT_COMMIT_MESSAGE
          AppName-msg: tarek